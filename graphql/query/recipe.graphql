query($id: String!) {
    recipe(id: $id) {
            id
            title
            description,
        translations {
            name,
            locale
        },
        translate @skip(if: false) {
            name
        },
        ingilizce: translate {
            name
        }
    }
}
