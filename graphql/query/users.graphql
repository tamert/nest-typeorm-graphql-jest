query {
    users(page: 1, limit: 5)  {
        totalCount,
        currentPage,
        totalPages,
        pageInfo {
            hasNextPage
            hasPrevPage
        }
        nodes {
            id
            email
        }
    }
}
