query($id: String!) {
    recipe(id: $id) {
            id
        translations {
            name,
            description,
            locale
        },
        translate {
            name,
            description
        }
    }
}
