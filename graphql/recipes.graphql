query {
    recipes(page: 1, limit: 5) {
        totalCount,
        currentPage,
        totalPages,
        pageInfo {
            hasNextPage
            hasPrevPage
        }
        items {
            id
            title
            creationDate
        }
    }
}