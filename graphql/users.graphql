query {
    users(page: 1, limit: 5)  {
        totalCount,
        currentPage,
        totalPages,
        pageInfo {
            hasNextPage
            hasPrevPage
        }
        items {
            id
            email
        }
    }
}